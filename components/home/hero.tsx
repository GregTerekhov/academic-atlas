import { SectionTemplate } from 'template';
import { PrimaryButtonUI } from 'ui';

export default function Hero() {
  return (
    <SectionTemplate>
      <p>Герой</p>
      <PrimaryButtonUI />
    </SectionTemplate>
  );
}
