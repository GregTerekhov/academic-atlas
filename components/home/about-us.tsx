import { SectionTemplate } from 'template';
import { SvgIconUI } from 'ui';

export default function AboutUs() {
  return (
    <SectionTemplate>
      <p>Про нас</p>
      <SvgIconUI />
      <SvgIconUI />
      <SvgIconUI />
    </SectionTemplate>
  );
}
